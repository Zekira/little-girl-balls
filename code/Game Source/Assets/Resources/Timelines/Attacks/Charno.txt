#This is a comment
//This is also a comment
//These files are used to create attack patterns and stages runtime, while still being fairly easy to edit
//Any newlines are disregarded everything starts after ;// (and the file start. (that // is solely there to comment this bit out)) and end in a ;
//You cannot indent
//These are the implemented functions:
//bulletProperty(name,property,value[,values..]), createBullet(name), wait(value), set(name,value), add(name,value,value), sub(name,value,value), mul(name,value,value), div(name,value,value),
mod(name,value,value), pow(name,value,value), sin(name,value), cos(name,value), tan(name,value), abs(name,value), asin(name,value), acos(name,value), atan(name,value);
//name is what you refer a variable by;
//value is either a number or a name. can't be a function;
//property is one of the following: movement (2 values), isharmful, scale, id, colorise, color(4 values);
#Without further ado, here we go!;

setTime(1200);
setParentMaxHealth(80);
setParentHealth(80);
bulletProperty(bullet, scale, 0.3);
set(rotation, 3.14);
REPEAT(10);
REPEAT(50);
REPEAT(4);
sin(rotatedx, rotation);
mul(rotatedx, rotatedx, 0.05);
cos(rotatedy, rotation);
mul(rotatedy, rotatedy, 0.05);
bulletProperty(bullet, movement, rotatedx, rotatedy);
createBullet(bullet);
add(rotation, rotation, 0.81);
ENDREPEAT;
wait(3);
//moveParent(0, -0.02);
add(rotation, rotation, 0.3);
ENDREPEAT;
wait(2);
REPEAT(50);
REPEAT(16);
sin(rotatedx, rotation);
mul(rotatedx, rotatedx, 0.05);
cos(rotatedy, rotation);
mul(rotatedy, rotatedy, 0.05);
bulletProperty(bullet, movement, rotatedx, rotatedy);
createBullet(bullet);
add(rotation, rotation, 0.392699);
ENDREPEAT;
wait(3);
add(rotation, rotation, -0.3);
ENDREPEAT;
wait(2);
ENDREPEAT;